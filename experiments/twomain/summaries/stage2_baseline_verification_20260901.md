# 阶段二基线验证记录（2026-09-01）

## 一、目的

验证阶段二建立的配置文件和统一运行器能够在不修改解码流程的条件下，严格复现冻结代码基线 `ff2f2d5` 的三组 TwoMain 参考结果。

本次工作区相对于 `ff2f2d5` 只有已提交的候选方案文档以及尚未提交的阶段二实验框架文件；`apps/`、`include/`、`src/` 和 `CMakeLists.txt` 中没有解码代码差异。

## 二、共同条件

```text
Eb/N0                    = 3.065 dB
信息 bit 数              = 60,014,592
Post-FEC 比较 bit 数     = 58,608,000
bit/channel seed         = 20260319 / 3182026
LLR                      = qfloat<6>
五六级共享               = 开启
Buffered FIFO            = 开启
R_buf                    = 16,000 block row
帧尾排空                 = 开启
调度                     = Group4 LoadSortedMultiround
每个时刻 group-entry     = 8
```

## 三、验证结果

| 编号 | HISO 类别掩码 | HISO/SISO | Post-FEC | 完成 batch | 强制顶出 | 最大 FIFO 深度 | 冻结签名 |
|---|---:|---:|---:|---:|---:|---:|---|
| `TM-A0` | `15` | `8/8` | `159 / 58,608,000` | `16,897` | `0` | `1,888` | 通过 |
| `TM-A1` | `7` | `8/8` | `9 / 58,608,000` | `16,897` | `0` | `1,923` | 通过 |
| `TM-A2` | `0` | `0/8` | `11 / 58,608,000` | `16,897` | `0` | `4,476` | 通过 |

冻结签名不是只比较最终错误数，还同时比较：

- Post-FEC 比较 bit 数；
- 完整错误位置文件 SHA-256；
- Level 5 的 EarlyStop、HISO、SISO、Unscheduled 数量；
- Level 6 的 EarlyStop、HISO、SISO、Unscheduled 数量；
- 共享入口、HISO、SISO和两类空闲资源总量；
- ParityOnly、OneMain、OneMainPlusParity、TwoMain、HardFail 的动作数量；
- 完成 batch 数、强制顶出数和最大 FIFO 深度。

三组均与 2026-08-26 的历史结果逐项一致。

## 四、快速默认行为回归

另建立 `TM-A0-QUICK` 固定短帧回归。它使用同一随机种子和原始 TwoMain 策略，信息 bit 数为 `1,818,624`，`R_buf=512`。

冻结签名为：

```text
Post-FEC                    = 0 / 412,032
完成 batch                 = 513
强制顶出                   = 0
最大 FIFO 深度             = 18
Level 5 动作               = [13765, 598, 2053, 576]
Level 6 动作               = [16361, 0, 55, 576]
共享入口/HISO/SISO/空闲量  = [2108, 598, 2108, 3650, 2140]
```

快速回归已经重复运行并通过。以后新增 TwoMain 参数或策略后，只要未显式启用新行为，该签名必须保持不变。

## 五、结论

阶段二的三组配置不是形式上的配置模板，而是已经通过完整长帧复现的可执行基线。统一运行器能够隔离原始输出、保存完整运行清单、解析 BER 和调度统计，并自动判断结果是否与冻结签名一致。

阶段二没有修改 TwoMain 算法。后续进入阶段三时，可以在同一实验框架中增加方案六参数，并使用本记录中的三组基线和快速回归检查新代码是否只改变目标机制。
