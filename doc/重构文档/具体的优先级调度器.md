TwoErrorOnly类
属性：

方法：
    run()
        根据混合类分类器输出的结果，筛选出TwoMain类型
        把TwoMain类型按优先级放入可回收列表
        输出优先级调度后的结果


OneAndTwoErrorPriority类
属性：

方法：
    run()
        根据混合类分类器输出的结果，筛选出OneMain、OneMainPlusParity、TwoMain类型
        按 OneMain > OneMainPlusParity > TwoMain 的顺序排列优先级
        输出优先级调度后的结果


ParityOneAndTwoErrorPriority类
属性：

方法：
    run()
        根据混合类分类器输出的结果，筛选出ParityOnly、OneMain、OneMainPlusParity、TwoMain类型
        按 ParityOnly > OneMain > OneMainPlusParity > TwoMain 的顺序排列优先级
        输出优先级调度后的结果
