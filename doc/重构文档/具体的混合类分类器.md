RepoFastClassifier类
属性：

方法：
    run()
        根据输入的code[N,256]矩阵计算硬判结果
        根据硬判结果计算s0、s1、s3
        根据s0、s1、s3判断这一行属于 Clean、ParityOnly、OneMain、OneMainPlusParity、TwoMain、HardFail 中的哪一种
        返回混合类分类结果


S1S3Classifier类
属性：

方法：
    run()
        根据输入的code[N,256]矩阵计算硬判结果
        根据硬判结果计算s0、s1、s3
        根据s1、s3以及Trace(mu)判断这一行属于 Clean、ParityOnly、OneMain、OneMainPlusParity、TwoMain、HardFail 中的哪一种
        返回混合类分类结果


S1S3WithS0Classifier类
属性：

方法：
    run()
        根据输入的code[N,256]矩阵计算硬判结果
        根据硬判结果计算s0、s1、s3
        根据s1、s3以及Trace(mu)判断这一行属于 Clean、ParityOnly、OneMain、OneMainPlusParity、TwoMain、HardFail 中的哪一种
        同时使用s0约束两错入口
        返回混合类分类结果
