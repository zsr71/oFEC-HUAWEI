
依照校验式判别早停类
属性：

方法：
    run():
        根据输入的code[N,256]矩阵计算前255校验和奇偶校验
        根据前255校验和奇偶校验返回早停结果[N]


根据基于 LLR 可靠度计数早停类
属性：
    早停幅度阈值
    早停个数阈值

方法：
    run()
        根据早停幅度阈值、输入的code[N,256]矩阵统计不可靠比特的个数
        根据不可靠比特的个数来返回早停结果